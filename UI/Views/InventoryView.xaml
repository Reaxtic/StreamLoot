<UserControl x:Class="UI.Views.InventoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:local="clr-namespace:UI"
             mc:Ignorable="d"
             d:DesignHeight="900" d:DesignWidth="1200"
             Background="{DynamicResource MainBackgroundBrush}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <Grid Margin="0,0,0,20">
            <TextBlock Text="Inventory"
                   FontSize="30"
                   FontWeight="Bold"
                   HorizontalAlignment="Left"
                   Foreground="{DynamicResource TextPrimaryBrush}"/>
            <Button Content="Auto (mine by priority)"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"
                    Padding="12,6"
                    Click="OnAutoMineClick"
                    Style="{StaticResource AccentButtonStyle}"
                    ToolTip="Clear any pinned campaign and select automatically"/>
        </Grid>

        <!-- CAMPAIGNS LIST -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding ActiveCampaigns}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{DynamicResource CardBackgroundBrush}"
                                CornerRadius="16"
                                Margin="0,0,0,20"
                                Padding="20"
                                BorderBrush="{DynamicResource BorderBrush}"
                                BorderThickness="1">

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Game Image -->
                                <Border Width="100" Height="100" CornerRadius="10">
                                    <Border.Background>
                                        <SolidColorBrush Color="{DynamicResource CardBackgroundColor}"/>
                                    </Border.Background>

                                    <Border.Clip>
                                        <RectangleGeometry RadiusX="10" RadiusY="10" Rect="0,0,100,100"/>
                                    </Border.Clip>
                                    
                                    <Image local:ImageEx.SourceUrl="{Binding GameImageUrl}" Stretch="UniformToFill" RenderOptions.BitmapScalingMode="HighQuality"/>
                                </Border>                                

                                <!-- Campaign Info -->
                                <StackPanel Grid.Column="1" Margin="20,0,0,0" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding GameName}"
                                               FontSize="22"
                                               FontWeight="Bold"
                                               Foreground="{DynamicResource TextPrimaryBrush}"/>

                                    <TextBlock Text="{Binding Name}"
                                               FontSize="14"
                                               Foreground="{DynamicResource TextMutedBrush}"
                                               Margin="0,4,0,10"/>

                                    <!-- Platform Badge + End Time + Watching Badge -->
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <!-- Platform Badge - Kick = default (green), Twitch = purple -->
                                        <Border CornerRadius="6" Padding="10,4">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <!-- Default = Kick green -->
                                                    <Setter Property="Background">
                                                        <Setter.Value>
                                                            <SolidColorBrush Color="{DynamicResource KickGreen}"/>
                                                        </Setter.Value>
                                                    </Setter>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Platform}" Value="Twitch">
                                                            <Setter Property="Background">
                                                                <Setter.Value>
                                                                    <SolidColorBrush Color="{DynamicResource TwitchPurple}"/>
                                                                </Setter.Value>
                                                            </Setter>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>

                                            <TextBlock Text="{Binding Platform}"
                                               FontWeight="UltraBold"
                                               Foreground="Black"
                                               FontSize="12"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"/>
                                        </Border>

                                        <!-- Campaign type: General (any live channel of the game) vs Channel-specific (only listed channels) -->
                                        <Border CornerRadius="6" Padding="10,4" Margin="8,0,0,0" VerticalAlignment="Center">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <!-- Channel-specific = muted -->
                                                    <Setter Property="Background" Value="#33FFFFFF"/>
                                                    <Setter Property="ToolTip" Value="Channel-specific: progresses only on the channels listed for this campaign."/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsGeneralDrop}" Value="True">
                                                            <Setter Property="Background" Value="#3300FFAA"/>
                                                            <Setter Property="ToolTip" Value="General: progresses on ANY live channel of the game, no matter which one you watch."/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <TextBlock FontWeight="SemiBold" FontSize="11"
                                                       Foreground="{DynamicResource TextPrimaryBrush}"
                                                       HorizontalAlignment="Center" VerticalAlignment="Center">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Text" Value="Channel-specific"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsGeneralDrop}" Value="True">
                                                                <Setter Property="Text" Value="General"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </Border>

                                        <TextBlock Margin="15,0,0,0" Foreground="#FF6666">
                                            <Run Text="Ends "/>
                                            <Run Text="{Binding EndsAt, StringFormat=MMM dd • HH:mm}"/>
                                        </TextBlock>

                                        <!-- WATCHING Badge -->
                                        <Border Margin="12,0,0,0"
                                                CornerRadius="6"
                                                Padding="8,4"
                                                Background="{DynamicResource SecondaryAccentBrush}"
                                                Visibility="{Binding IsCurrentCampaign, Converter={StaticResource BooleanToVisibilityConverter}}">
                                            <TextBlock Text="WATCHING"
                                                       FontWeight="Bold"
                                                       FontSize="10"
                                                       Foreground="{DynamicResource MainBackgroundBrush}"/>
                                        </Border>

                                        <!-- Force-mine this campaign -->
                                        <Button Content="Mine this"
                                                Margin="12,0,0,0"
                                                Padding="10,4"
                                                Click="OnMineThisClick"
                                                Style="{StaticResource AccentButtonStyle}"/>
                                    </StackPanel>
                                </StackPanel>

                                <!-- Rewards List -->
                                <StackPanel Grid.Column="2" VerticalAlignment="Top" MinWidth="320">
                                    <TextBlock Text="Rewards"
                                               FontSize="14"
                                               Foreground="{DynamicResource TextSecondaryBrush}"
                                               Margin="0,0,0,8"/>

                                    <ItemsControl ItemsSource="{Binding Rewards}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Background="{DynamicResource BorderBrush}"
                                                        CornerRadius="10"
                                                        Margin="0,0,0,8"
                                                        Padding="12">

                                                    <Border.Style>
                                                        <Style TargetType="Border">
                                                            <Style.Triggers>
                                                                <!-- Highlight active reward -->
                                                                <DataTrigger Binding="{Binding IsCurrentReward}" Value="True">
                                                                    <Setter Property="BorderBrush" Value="{DynamicResource SecondaryAccentBrush}"/>
                                                                    <Setter Property="BorderThickness" Value="1.69"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>

                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="Auto"/>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>

                                                        <!-- Reward Image -->
                                                        <Border Grid.Column="0" Width="50" Height="50" CornerRadius="6">
                                                            <Border.Background>
                                                                <SolidColorBrush Color="{DynamicResource CardBackgroundColor}"/>
                                                            </Border.Background>
                                                            
                                                            <Border.Clip>
                                                                <RectangleGeometry RadiusX="6" RadiusY="6" Rect="0,0,50,50"/>
                                                            </Border.Clip>
                                                            
                                                            <Image local:ImageEx.SourceUrl="{Binding ImageUrl}" Stretch="UniformToFill"/>
                                                        </Border>

                                                        <!-- Name + Progress -->
                                                        <StackPanel Grid.Column="1" Margin="12,0,0,0" VerticalAlignment="Center">
                                                            <TextBlock Text="{Binding Name}"
                                                                       FontWeight="Bold"
                                                                       Foreground="{DynamicResource AccentBrush}"/>

                                                            <TextBlock Margin="0,6,0,0" Foreground="{DynamicResource TextPrimaryBrush}" FontWeight="SemiBold">
                                                                <Run Text="{Binding ProgressPercent, Mode=OneWay}" Foreground="{DynamicResource AccentBrush}"/>
                                                                <Run Text="% • "/>
                                                                <Run Text="{Binding ProgressMinutes}" Foreground="{DynamicResource AccentBrush}"/>
                                                                <Run Text=" / "/>
                                                                <Run Text="{Binding RequiredMinutes}"/>
                                                                <Run Text=" min"/>
                                                            </TextBlock>

                                                            <!-- Progress Bar -->
                                                            <ProgressBar Value="{Binding ProgressMinutes}"
                                                                         Maximum="{Binding RequiredMinutes}"
                                                                         Height="8"
                                                                         Margin="0,8,0,0"
                                                                         Foreground="{DynamicResource AccentBrush}"
                                                                         Background="#333333"/>
                                                        </StackPanel>

                                                        <!-- CLAIMED Badge -->
                                                        <Border Grid.Column="2" Background="{DynamicResource AccentBrush}"
                                                                CornerRadius="6"
                                                                Padding="8,4"
                                                                Margin="10,0,0,0"
                                                                Visibility="{Binding IsClaimed, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                            <TextBlock Text="CLAIMED"
                                                                       FontWeight="Bold"
                                                                       FontSize="10"
                                                                       Foreground="{DynamicResource MainBackgroundBrush}"/>
                                                        </Border>

                                                        <!-- WATCHING Badge -->
                                                        <Border Grid.Column="2" Background="{DynamicResource SecondaryAccentBrush}"
                                                                CornerRadius="6"
                                                                Padding="8,4"
                                                                Margin="10,0,0,0"
                                                                Visibility="{Binding IsCurrentReward, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                            <TextBlock Text="WATCHING"
                                                                       FontWeight="Bold"
                                                                       FontSize="10"
                                                                       Foreground="{DynamicResource MainBackgroundBrush}"/>
                                                        </Border>
                                                    </Grid>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>

                <!-- EMPTY STATE - 100% SAFE -->
                <ItemsControl.Style>
                    <Style TargetType="ItemsControl">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate>
                                    <ItemsPresenter/>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ActiveCampaigns.Count}" Value="0">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate>
                                            <TextBlock Text="No active drops campaigns"
                                                       FontSize="26"
                                                       Foreground="{DynamicResource TextMutedBrush}"
                                                       HorizontalAlignment="Center"
                                                       VerticalAlignment="Center"/>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ItemsControl.Style>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>